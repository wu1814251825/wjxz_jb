var content1 = getClip();
if (content1 != null) {
    toast("剪贴板内容为：" + content1);
} else {
    toast("剪贴板为空");
}

var content2 = getClip();
if (content2 != null) {
    toast("剪贴板内容为：" + content2);
} else {
    toast("剪贴板为空");
}

